# local scan artifacts / reports
*.report.html
/tmp/
.DS_Store
# generated launchd plist (created by install.sh)
launchd/*.plist
# tooling scratch (never commit)
.serena/
.playwright-mcp/
# local dev notes for Claude (private)
CLAUDE.md
